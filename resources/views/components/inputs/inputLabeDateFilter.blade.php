<label for="{{ $inputId }}">{{ $labelTitle }}</label>
<input type="text" name="{{ $inputName }}" id="{{ $inputId }}" class="form-input-class ts_datepicker"
    placeholder="{{ $labelTitle }}" readonly />
